# Core dependencies
wheel
setuptools

# Web framework - updated for cohere v5 compatibility
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.6.0

# Embeddings API (replaces sentence-transformers - saves 300MB RAM!)
cohere>=5.9.0,<6.0.0

# Vector DB and LLM
qdrant-client==1.7.3
groq>=0.4.0
